{
  "name": "blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "git submodule update --init && echo `ls -R ./` && echo `ls /` && rm -rf ./open-wiki/.git && mv ./open-wiki /vercel/output && node scripts/prebuild.js",
    "dev": "yarn run prebuild && next dev",
    "build": "yarn run prebuild && next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "dayjs": "^1.11.10",
    "gray-matter": "^4.0.3",
    "marked": "^11.1.0",
    "next": "14.0.4",
    "react": "^18",
    "react-dom": "^18",
    "recursive-readdir": "^2.2.3",
    "remark": "^15.0.1",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@types/recursive-readdir": "^2.2.4",
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "14.0.4",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}
